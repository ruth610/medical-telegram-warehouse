# Optional dbt packages
# Run 'dbt deps' to install these packages
# Note: Current models work without dbt_utils, but you can install it for additional utilities
packages:
  # - package: dbt-labs/dbt_utils
  #   version: 1.1.1
